#include "main.h"

/**
 * mul - multipies two integers
 * @c: the first number to multiply
 * @a: the second number to multiply with the first number
 *
 * Return: the result of multiplication between c param and a param
 */
int mul(int c, int a)
{
return (c * a);
}
